{
  "name": "fieldops",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:backend": "npm run start:dev --prefix backend",
    "build": "tsc -b && vite build",
    "build:backend": "npm run build --prefix backend",
    "test": "vitest run",
    "test:backend": "npm test --prefix backend",
    "test:e2e:backend": "npm run test:e2e --prefix backend",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "typescript": "~5.9.3",
    "vite": "^8.0.1",
    "vitest": "^4.1.5"
  }
}
